# How is the restart strategy determined?

1. was launcher specified by the programmer? _yes_: use this
2. is runnable jar in base dir? _yes_: use this
3. is there exactly 1 (not more!) runnable jar anywhere in the update directory? _yes_: use this

